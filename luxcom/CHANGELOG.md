# LuxCom 원격 클라이언트 빌드 기록

> 버전 표기: `v1.4.6.N` = RustDesk 베이스(1.4.6) + 우리 빌드번호 N(kLuxComBuild). 소비자/기사 좌패널 하단에 표시.
> (구 형식 `MMDD-N` 은 0608-3 까지) 패치할 때마다 N을 올리고 여기에 기록한다.

## v1.4.6.4 (2026-07-27)
- **[버그수정] '윈도우 공유문제 해결' 적용 시 Workstation(SMB 클라이언트) 서비스가 중지되던 치명 버그 수정**: 적용 스크립트가 LanmanWorkstation을 시작한 직후 곧바로 `Restart-Service -Force`를 호출 → START_PENDING 경합으로 재시작의 Start 단계가 무시되어 서비스가 '중지'된 채 남았음(`-EA 0`로 은폐). Workstation이 중지되면 그 PC는 공유폴더·NAS 접근 자체가 불가 → 기능이 목적과 정반대로 동작. 수정: ①자동시작 목록에서 LanmanWorkstation 제외(경합 원천 제거) ②이중 `Restart-Service` 삭제 → 안전 재시작(Stop→완전정지 대기→Start, Running까지 최대 3회 재시도→의존 서비스 Browser 복구) ③'원래대로 복구'(winfix_restore.ps1)에도 같은 안전 재시작 적용(복구를 눌러도 재발하던 문제 차단). 레지스트리 변경(게스트 허용·서명 해제)은 그대로 — 서비스 중지는 재시작 로직 버그였을 뿐 SMB 설정과 무관.
- 버전 v1.4.6.3 → v1.4.6.4 (kLuxComBuild 3→4). (consumer·staff 공통)

## v1.4.6.3 (2026-06-11)
- **PC UUID(MachineGuid) 인식·전송**: 기사 로그인/세션 복귀 시 PC 고유 UUID를 서버로 전송(콘솔창 없이 레지스트리 RegGetValueW). 서버가 동시접속 세션을 IP+UUID로 식별 → 관리자 콘솔 계정별 '접속 기기' 목록·로그인 감사에 표시.
- **내 채널 표시**: 기사 좌패널에 로그인 응답의 채널 번호 표시('내 채널: N'). 서버 자동승인 정책으로 부여된 채널 확인.
- **윈도우 공유문제 해결 메뉴**(기사 좌패널 '윈도우 공유문제 해결'): Win11 24H2/25H2의 NAS·공유폴더·프린터 끊김을 한 번에 수정 — ①SMB 게스트 접근 허용 ②SMB 서명 강제 해제 ③네트워크 검색+공유 방화벽·서비스 ④프린터 공유 0x11b 오류. **관리자 권한(UAC)로 실행, 적용 전 현재값 백업→'원래대로 복구' 제공**. SMB1은 보안상 제외. 각 항목 친절 설명.
- 버전 v1.4.6.2 → v1.4.6.3 (kLuxComBuild 2→3). (staff 프로필 대상)

## v1.4.6.2 (2026-06-09)
- **광고 전면 제거**: 시작 시 자체광고창 + 좌패널 홍보배너 모두 삭제(소비자·기사). 클라이언트에 광고 일절 없음.
- **"이 PC 정보" 창 가로 2배(600px) 확대** — 소비자(좁은 창)는 열 때 창을 잠시 넓히고 닫을 때 복원.
- **PC 정보에 RAM·그래픽카드·메인보드·바이오스 버전 추가** — 콘솔창 없이 Win32 API/레지스트리(GlobalMemoryStatusEx·RegGetValueW HKLM, dart:ffi)로 수집. 못 읽으면 조용히 생략.

## v1.4.6.1 (2026-06-09, 구 빌드 0608-4)
- **접속현황 실시간화**: 소비자 보고 15→5초, 기사 폴링 4→2초, 서버 만료(PRESENCE_MS) 40→16초 → 접속/정상해제 ~2초, 비정상 종료도 ~16초면 대기목록에서 사라짐(기존 최대 40초).
- **소비자 창 확대**: 폭 280→320(높이는 콘텐츠 자동), '이 PC 정보' 버튼 잘 보임.
- **소비자 ⋮(설정) 제거**: 고객이 설정에 못 들어가게(기사용은 유지).
- **버전 표시 신설**: `v1.4.6.1`(+작게 'RustDesk 1.4.6 기반') — 소비자·기사 패널 하단. 스탠바이 카드의 기존 '버전…빌드' 라인 제거(채널만 유지).
- **광고/홍보 슬롯 자리 마련**: 소비자·기사 좌패널. 데스크탑 앱은 애드센스/3rd-party 광고 금지(정책·백신)라 자체 쇼핑몰 배너(클릭→luxcom.co.kr) placeholder, 추후 콘텐츠만 교체.

## 0608-3 (2026-06-08)
- **이 PC 정보** 버튼 추가(소비자 스탠바이 카드 + 기사 좌패널) — PC 이름·도메인/워크그룹·내부 IP·**외부 IP**·Windows 버전·비트·CPU·논리 코어를 한 화면에서 확인 + 전체 복사. 기사가 원격 붙은 PC 환경(특히 IP)을 장치 설치 등에서 즉시 파악. 외부 IP는 luxauth `/api/ip`(공인 IP 반환) 사용, 순수 Dart 수집이라 콘솔창 안 뜸. 소비자·기사 둘 다 재빌드.

## 0608-2 (2026-06-08, staff)
- 기사용 로그인 게이트에 **회원가입 / 비밀번호 찾기·변경 링크** 추가 — 기본 브라우저로 `405.kr/signup/`, `405.kr/luxauth/forgot` 열기(url_launcher). staff 빌드만 해당.
- (luxauth 측: 사장님이 Gmail 앱비번 설정 완료 → 비밀번호 재설정 메일 발송 작동.)

## 0608-1 (2026-06-08)
- **스탠바이 끄면 즉시 기사 대기목록에서 제거**(offline 통지) + 이전 PC 이름(이력) 클리어 — 껐는데 옛 번호·이름 남던 문제.
- **스탠바이 켜면 창을 트레이로 숨김**(작업표시줄에서 사라짐) — `_enable`에 `windowManager.hide()` 추가. 트레이 아이콘은 설치 상태에서 RustDesk 자동.
- **기사 대기목록 새로고침 8초→4초** (5초 이내 요청).
- **버전 빌드 태그 표시**(소비자 스탠바이 카드 + 기사 좌패널).

## ae13e77 (2026-06-07)
- 채널 자동감지(remote-Nc → 채널 N, 포터블 RUSTDESK_APPNAME 환경변수).
- presence 항상 보고(스탠바이 OFF여도 앱 켜져 있으면 기사 목록에 표시).
- 기사 목록 상주(초록)/일반(주황) 배지 + 점 색 구분.
- 종료 시 offline 즉시 통지 + presence 만료 90→40초.
- 스탠바이 시작/닫기 시 창 트레이로 숨김.

## 9d16484 (2026-06-07)
- EXE 파일 아이콘 navy 405(포터블 res/icon.ico).
- 소비자 홈 정리(쇼핑몰/연락처 제거 → 버전·채널 표시).
